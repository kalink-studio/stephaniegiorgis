name: Release

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'Image tag to deploy (commit SHA or version tag)'
        required: true
        type: string
      environment:
        description: 'Target environment'
        required: true
        type: choice
        options:
          - staging
          - production

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/kalink-studio/stephaniegiorgis

jobs:
  detect-release:
    name: Detect Release Commit
    if: github.event_name == 'workflow_run'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read

    outputs:
      should_deploy: ${{ steps.detect.outputs.should_deploy }}
      release_sha: ${{ steps.detect.outputs.release_sha }}

    steps:
      - name: Detect merged changeset release commit
        id: detect
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const run = context.payload.workflow_run

            if (run.conclusion !== 'success' || run.event !== 'push' || run.head_branch !== 'main') {
              core.setOutput('should_deploy', 'false')
              core.setOutput('release_sha', '')
              return
            }

            const { data: pulls } = await github.rest.repos.listPullRequestsAssociatedWithCommit({
              owner: context.repo.owner,
              repo: context.repo.repo,
              commit_sha: run.head_sha,
            })

            const releasePr = pulls.find((pull) => {
              return pull.merged_at &&
                pull.base?.ref === 'main' &&
                pull.head?.ref?.startsWith('changeset-release/') &&
                pull.merge_commit_sha === run.head_sha
            })

            core.setOutput('should_deploy', releasePr ? 'true' : 'false')
            core.setOutput('release_sha', releasePr ? run.head_sha : '')

  deploy-production:
    name: Deploy to Production
    if: github.event_name == 'workflow_run' && needs.detect-release.outputs.should_deploy == 'true'
    needs: detect-release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: ${{ needs.detect-release.outputs.release_sha }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify sharp
        run: node -e "require('sharp'); console.log('sharp ok')"

      - name: Run production Payload migrations
        env:
          PAYLOAD_MIGRATING: 'true'
          DATABASE_URL: ${{ secrets.PRODUCTION_DATABASE_URL }}
          DATABASE_URL_DIRECT: ${{ secrets.PRODUCTION_DATABASE_URL_DIRECT }}
          PAYLOAD_SECRET: ${{ secrets.PRODUCTION_PAYLOAD_SECRET }}
          PAYLOAD_PUBLIC_SERVER_URL: ${{ secrets.PRODUCTION_PAYLOAD_PUBLIC_SERVER_URL }}
        run: pnpm payload migrate

      - name: Get version from package
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "value=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"

      - name: Get commit SHA for release
        id: sha
        run: |
          COMMIT_SHA=$(git rev-parse HEAD)
          SHORT_SHA=$(git rev-parse --short=7 "$COMMIT_SHA")
          echo "short=$SHORT_SHA" >> "$GITHUB_OUTPUT"
          echo "full=$COMMIT_SHA" >> "$GITHUB_OUTPUT"

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Re-tag image with version
        run: |
          docker pull ${{ env.IMAGE_NAME }}:${{ steps.sha.outputs.short }}
          docker tag ${{ env.IMAGE_NAME }}:${{ steps.sha.outputs.short }} ${{ env.IMAGE_NAME }}:${{ steps.version.outputs.tag }}
          docker push ${{ env.IMAGE_NAME }}:${{ steps.version.outputs.tag }}

      - name: Update production kustomization
        run: |
          sed -i "s|newTag: .*|newTag: ${{ steps.version.outputs.tag }}|" k8s/overlays/production/kustomization.yaml

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add k8s/overlays/production/kustomization.yaml
          if git diff --staged --quiet; then
            echo "No production kustomization change to commit"
            exit 0
          fi
          git commit -m "chore(production): deploy ${{ steps.version.outputs.tag }}"
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main

  manual-deploy:
    name: Manual Deploy
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify sharp
        run: node -e "require('sharp'); console.log('sharp ok')"

      - name: Run Payload migrations
        env:
          STAGING_DATABASE_URL: ${{ secrets.STAGING_DATABASE_URL }}
          STAGING_DATABASE_URL_DIRECT: ${{ secrets.STAGING_DATABASE_URL_DIRECT }}
          STAGING_PAYLOAD_SECRET: ${{ secrets.STAGING_PAYLOAD_SECRET }}
          STAGING_PAYLOAD_PUBLIC_SERVER_URL: ${{ secrets.STAGING_PAYLOAD_PUBLIC_SERVER_URL }}
          PRODUCTION_DATABASE_URL: ${{ secrets.PRODUCTION_DATABASE_URL }}
          PRODUCTION_DATABASE_URL_DIRECT: ${{ secrets.PRODUCTION_DATABASE_URL_DIRECT }}
          PRODUCTION_PAYLOAD_SECRET: ${{ secrets.PRODUCTION_PAYLOAD_SECRET }}
          PRODUCTION_PAYLOAD_PUBLIC_SERVER_URL: ${{ secrets.PRODUCTION_PAYLOAD_PUBLIC_SERVER_URL }}
        run: |
          export PAYLOAD_MIGRATING=true

          if [ "${{ inputs.environment }}" = "staging" ]; then
            export DATABASE_URL="$STAGING_DATABASE_URL"
            export DATABASE_URL_DIRECT="$STAGING_DATABASE_URL_DIRECT"
            export PAYLOAD_SECRET="$STAGING_PAYLOAD_SECRET"
            export PAYLOAD_PUBLIC_SERVER_URL="$STAGING_PAYLOAD_PUBLIC_SERVER_URL"
          else
            export DATABASE_URL="$PRODUCTION_DATABASE_URL"
            export DATABASE_URL_DIRECT="$PRODUCTION_DATABASE_URL_DIRECT"
            export PAYLOAD_SECRET="$PRODUCTION_PAYLOAD_SECRET"
            export PAYLOAD_PUBLIC_SERVER_URL="$PRODUCTION_PAYLOAD_PUBLIC_SERVER_URL"
          fi

          pnpm payload migrate

      - name: Update kustomization
        run: |
          sed -i "s|newTag: .*|newTag: ${{ inputs.image_tag }}|" k8s/overlays/${{ inputs.environment }}/kustomization.yaml

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add k8s/overlays/${{ inputs.environment }}/kustomization.yaml
          if git diff --staged --quiet; then
            echo "No kustomization change to commit"
            exit 0
          fi
          git commit -m "chore(${{ inputs.environment }}): deploy ${{ inputs.image_tag }} [manual]"
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main
